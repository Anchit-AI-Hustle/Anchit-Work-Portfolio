{
  "name": "tencentdb-memory-mcp-server",
  "version": "1.0.0",
  "description": "MCP bridge that gives Claude Code persistent long-term memory via the TencentDB Agent Memory gateway (4-tier local memory pipeline).",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "tencentdb-memory-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "axios": "^1.7.9",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
